type User {
  id: String
  username: String
  boards: [Board]
  originals: [Media]
}

type Media {
  user: User
  id: String
  format: String
  width: Number
  height: Number
  sha256: String
}

type Clip {
  media: [Media]
  caption: String
}

type Board {
  title: String
  favorites: Number
  favorited: Boolean
  clips: [Clip]
}

Query {

}

Mutation {

}
