{
  "name": "@quipt/monorepo",
  "repository": "git@github.com:quipt/monorepo.git",
  "license": "Unlicense",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@angular/*",
      "**/@angular/*/**"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.42.1",
    "@typescript-eslint/parser": "^5.42.1",
    "eslint": "^8.27.0",
    "generate-lockfile": "^0.0.12",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "lint": "eslint .",
    "ci:web": "yarn workspace @quipt/web test --watch=false",
    "postinstall": "generate-lockfile --lockfile yarn.lock --package ./packages/media-handler/package.json --write ./packages/media-handler/yarn.lock --force --dev"
  }
}
